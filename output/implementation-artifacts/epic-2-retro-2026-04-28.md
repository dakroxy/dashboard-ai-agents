---
epic: 2
title: "Versicherungen & Due-Radar"
date: 2026-04-28
facilitator: Amelia (Developer)
participants: [Daniel Kroll, Alice (Product Owner), Charlie (Senior Dev), Dana (QA), Elena (Junior Dev)]
status: complete
---

# Retrospektive — Epic 2: Versicherungen & Due-Radar

## Abschluss-Metriken

| Kennzahl | Wert |
|---|---|
| Stories abgeschlossen | 9 / 9 (100 %) |
| Test-Wachstum | 477 → 692 (+215 neue Tests) |
| Code-Review-Patches insgesamt | ~50+ über alle Stories |
| Davon CRITICAL / HIGH | 6 (alle in Story 2.3 + 2.1) |
| Stories mit eigenem Review-Fix-Commit | 7 von 9 |
| Sauberste Story | 2.5 Due-Radar (2 Patches, kein Fix-Commit) |
| Schwerste Story | 2.3 Schadensfall (14 Patches inkl. CRITICAL) |
| Offene Defer-Einträge nach Epic | ~30 (davon ~5 cross-cutting wiederkehrend) |

## Erfolge

**`_load_accessible_object`-Helfer als strukturelle Lösung**
Story 2.1 baute den zentralen Helfer; alle 5 Policen-Routes nutzen ihn. Die Klasse "Endpoint ohne `accessible_object_ids`-Check" (Epic-1-Retro-Trigger) ist für URL-`object_id`-Pfade strukturell tot. Story 2.3 hat das Pattern aufgegriffen, Stories 2.4 + 2.6 + 2.7 ebenso.

**Story-2.0-Pattern als Vorlage für 2.4**
Der `objects:view_confidential`-Enforcement-Block aus Story 2.0 (Handler-Conditional + Endpoint-Dependency + Template-Wrap + In-Handler-Check für Edit) wurde in Story 2.4 für Menschen-Notizen 1:1 wiederverwendet. Story 2.4 hatte daraufhin nur 4 Review-Patches, alle in der Klasse "Test fehlt" — kein Architektur-Patch.

**3-Layer-Adversarial-Review hat reale CRITICAL-Bugs gefangen**
Pro Story laufen Blind Hunter, Edge Case Hunter und Acceptance Auditor parallel. Echte Treffer: `unit_id`-IDOR in 2.3 (HIGH), Tests die Text asserteten der nie gerendert wurde in 2.3 (CRITICAL), `Decimal("0")`-Truthiness-Bug in 2.8 (HIGH). Ohne Review wären die produktiv geworden.

**Test-Bedrock von +215 Tests**
Jede neue Sektion (Policen, Wartung, Schadensfall, Due-Radar, Versicherer-Liste/-Detail) hat Permission-Matrix-Coverage und Cross-Object-Guards. Ist der Boden für Epic 3.

**Vorhersage von Komplexitäts-Stories funktioniert**
Story 2.8 wurde aus Epic-1-Retro-P3 als "Komplexitäts-Story analog 1.4" markiert. Ergebnis: 5 Patches statt der 7-15 von Story 1.4. Boundary-Klassifikation in den Dev Notes hat den Implementing-Dev nachweislich aufmerksamer gemacht.

## Herausforderungen

**Test-Coverage-Lücken sind die mit Abstand häufigste Patch-Klasse**
Stories 2.1 (AuditLog-Assertion), 2.2 (Cross-Police-Variant + Lösch-Button-Sichtbarkeit), 2.3 (Tests-asserten-falsche-Texte CRITICAL + Inf/NaN/Comma-Decimal), 2.4 (3 fehlende Tests + max_length), 2.6 (HX-Header + invalid type/severity), 2.7 (Sort-Tiebreak), 2.8 (Heatmap-Flakiness am Monatsanfang). Klasse: "Initial-Implementation deckt Happy-Path; Reviewer findet Boundary-Tests." Roughly ~50 % aller Patches fallen in diese Klasse.

**IDOR-Klasse nur teilweise strukturell verhindert**
Der `_load_accessible_object`-Helfer aus Story 2.1 fängt URL-`object_id`-Pfade. Aber: Story 2.3 hatte trotzdem einen `unit_id`-IDOR (CRITICAL) — der FK kam aus dem Form-Body, nicht aus der URL. Story 2.1 hatte einen analogen Cross-Object-Policy-Check ad-hoc gebaut, ohne Helfer. Pattern fehlt für die zweite IDOR-Oberfläche.

**Numerische Edge-Cases recurrent**
`Decimal("0")` als None behandelt (Story 2.8 — Praemie 0 € rendert "–"), Inf/NaN/Overflow ungeprüft (2.3), Case-Sensitive-Sort `"axa" < "Allianz"` (2.7), Sort-Tiebreak fehlt → flackernde Reihenfolge bei gleichem Wert (2.7).

**HTMX-Spezifika werden bei Initial-Implementation unterschätzt**
`<article>` direkter Child von `<tbody>` → invalides HTML, Browser-Reparse bricht Deep-Link (2.1). Nested `<tbody>` durch `innerHTML`-Swap statt `outerHTML` (2.6). HX-Request-Header-Guard fehlt → Direkt-Navigation auf Fragment-URL bricht (2.6). 422-Renderer ohne Context-Vars (2.2). Form bleibt `hidden` nach 422-Validation-Error → User sieht Fehlertext ohne Form (2.1, deferred).

**Cross-Cutting-Defers wachsen ohne Triage**
`deferred-work.md` hat aktuell ~30+ Einträge. Wiederkehrend in mehreren Reviews:
- CSRF projektweit (4× erwähnt)
- `date.today()` ohne Europe/Berlin-Timezone (3× erwähnt)
- Negative `praemie` / `Schadensfall.amount` ohne DB-CHECK-Constraint (2×)
- Magic-Strings statt Konstanten für Severity-Levels (2×)
- Concurrent-Saves / Optimistic-Lock (3×)

Diese Items werden bei jedem Review als Defer markiert und gelangen nie auf den Tisch.

## Schlüssel-Erkenntnisse

### E1 — Helfer fangen die Klasse, die du erwartest. Form-Body ist die zweite Klasse.

`_load_accessible_object` schließt URL-`object_id`-IDOR strukturell. Aber jeder FK aus dem Form-Body (`unit_id`, `policy_id`, `dienstleister_id`, `versicherer_id`) ist eine separate IDOR-Oberfläche. Story 2.3 hatte daran einen CRITICAL-Patch.

**Konsequenz für Epic 3:** Zweiter Helfer `_check_fk_belongs_to_object(entity, obj)` für alle FK-Form-Body-Validations. Story 3.5/3.6 (Review-Queue mit `target_entity_type` + `target_entity_id`) bringt die nächste Iteration dieses Patterns.

### E2 — Adversarial-Reviewer findet Test-Lücken systematischer als der Implementing-Dev

Das ist arbeitsteilig richtig — aber das *Mengenverhältnis* (rund 50 % aller Patches sind Test-Coverage-Patches) zeigt, dass das Story-Template der Test-Konstruktion zu wenig Struktur vorgibt.

**Konsequenz für Epic 3:** Story-Template um eine Test-Checkliste erweitern, die der Dev-Agent vor Initial-Submit abarbeitet:
- Permission-Matrix 200 / 403 pro Route
- IDOR-Test pro FK aus URL **und** Form-Body
- Numerische Boundaries (Inf / NaN / Decimal-Zero-Truthiness / Overflow / negative Werte / Komma-Decimal)
- Date-Bounds + Timezone-stabile Test-Setups (`today.replace(day=15)`-Pattern aus Story 2.8)
- HTMX-422-Render mit allen Context-Vars + sticky Form-Daten

### E3 — Komplexitäts-Vorhersage funktioniert. Boundary-Klassifikation als Pflichtfeld.

Story 2.8 wurde gezielt als "Boundary-Story analog 1.4" markiert → 5 Patches statt 7-15.

**Konsequenz für Epic 3:** Boundary-Klassifikation (`crud` / `aggregation` / `encryption` / `external_api` / `permission` / `async_lifespan` / `cache_invalidation` / `row_lock`) als Pflichtfeld bei `bmad-create-story`. Story 3.3 (Pflegegrad-Score-Service mit Cache-Invalidation in Write-Gate) und 3.6 (Approve-Race) sind die Boundary-Kandidaten in Epic 3.

### E4 — Wiederkehrende Defer-Items sind eine epoche-übergreifende Form von "ich weiß bescheid, mache aber nichts"

Klassiker: `date.today()` ohne Timezone wird seit Epic 1 bemerkt, in jedem Aggregations-Review neu gemerkt, und ist nirgends zentral gefixt.

**Konsequenz für Epic 3:** `deferred-work.md` einmal pro Epoche triagen — was ist Pre-Prod-Blocker, was nicht? Die wiederkehrenden 5 cross-cutting Items als "Hardening-Stories" vor Prod-Rollout schedulen, oder zwei davon (`today_local()`-Helper + `Severity`-StrEnum) direkt vor Story 3.1 mitnehmen.

## Vorherige Retrospektive — Follow-Through Epic 1

| # | Commitment aus Epic-1-Retro | Status | Evidenz |
|---|---|---|---|
| C1 | Story 2.0 vor Story 2.4 (`view_confidential`-Enforcement) | ✅ erledigt | Story 2.0 lieferte Canonical Pattern, 2.4 hat 1:1 übernommen |
| P1 | Boundary-Stories explizit markieren | ⚠️ uneinheitlich | In 2.1 Dev Notes ja (Retro-Ref P2 zitiert), in 2.3 nicht — gerade die Story mit CRITICAL |
| P2 | Security-AC + `accessible_object_ids` auf allen neuen Routen | ✅ teilweise erfolgreich | `_load_accessible_object`-Helfer in 2.1 etabliert; alle 5 Policen-Routes nutzen ihn. Aber Story 2.3 hatte trotzdem einen `unit_id`-IDOR (Form-Body-Klasse), der durch URL-orientierten Helfer nicht gefangen wurde |
| P3 | Story 2.8 als "Komplexitäts-Story analog 1.4" markieren | ✅ erledigt | 2.8 wurde mit Sorgfalt gefahren — 5 Patches, deutlich weniger als 1.4 (15) |

**Lesart:** 3 von 4 Commitments umgesetzt. P2 hat die erwartete Klasse strukturell geschlossen; die nicht erwartete Klasse (Form-Body-FK) bleibt offen → Action P3 in dieser Retro. P1 wurde uneinheitlich angewendet → Action P2 in dieser Retro macht es zum Pflichtfeld.

## Epic 3 Vorschau: Portfolio-UX & KI-Governance-Oberfläche

Epic 3 ist UX- und Governance-fokussiert. Keine neuen externen API-Anbindungen, keine neuen Encryption-Pfade. Aber zwei Stories mit echter Komplexität.

**Dependencies auf Epic 2:** minimal. Sort-Logik (3.1) baut auf Tabellen-Patterns aus 2.7. Cache-Invalidation (3.3) greift in Write-Gate ein, das seit Epic 1 stabil ist.

**Risiko-Profil pro Story:**

| Story | Boundary-Klasse | Risiko |
|---|---|---|
| 3.1 Objekt-Liste Sort/Filter | aggregation | Medium — Sort-Stabilität & Case-Folding (Lehre aus 2.7) |
| 3.2 Mobile Card-Layout | crud | Low — UI-Polish |
| 3.3 Pflegegrad-Score-Service | **cache_invalidation** | **High** — neue pure-Service-Domain, Cache-Invalidation greift in Write-Gate ein, analog 1.4 Scheduler-Komplexität |
| 3.4 Pflegegrad-Badge & Popover | crud | Low–Medium — UI auf 3.3 |
| 3.5 Review-Queue-Admin-UI | permission | Medium — Filter, Liste, leeres v1 |
| 3.6 Review-Queue Approve/Reject | **row_lock** | **High** — Approve-Race auf gleichem Feld, `superseded`-Auto-Promotion |

## Action Items

### Prozess-Verbesserungen (für Epic 3 anwenden)

| # | Action | Owner | Kriterium |
|---|---|---|---|
| P1 | **Test-Checkliste in Story-Template** verankern: (a) Permission-Matrix 200 / 403 pro Route, (b) IDOR-Test pro FK aus URL **und** Form-Body, (c) Numerische Boundaries (Inf / NaN / Decimal-Zero-Truthiness / Overflow / negative / Komma-Decimal), (d) Date-Bounds + Timezone-stabile Test-Setups (`today.replace(day=15)`-Pattern), (e) HTMX-422-Render mit allen Context-Vars + sticky Form-Daten | Amelia bei `bmad-create-story` 3.x | Jede neue Story-Spec hat den Block "AC: Tests" mit dieser Checkliste |
| P2 | **Boundary-Klassifikation als Pflichtfeld** im Story-Header: `boundary_class: crud / aggregation / encryption / external_api / permission / async_lifespan / cache_invalidation / row_lock`. Bei Nicht-CRUD: erhöhte Test-Erwartung + Review-Aufmerksamkeit | Amelia bei `bmad-create-story` | Jede 3.x-Story hat das Feld; 3.3 (`cache_invalidation`) und 3.6 (`row_lock`) explizit markiert |
| P3 | **`_check_fk_belongs_to_object`-Helfer** zentral in `app/routers/objects.py`: nimmt FK-UUID + Parent-Object, lädt Entity, prüft `entity.object_id == obj.id`, wirft 404 sonst. Gegenstück zu `_load_accessible_object` für die Form-Body-Klasse | Charlie vor Story 3.5 | Story 3.5 / 3.6 nutzen den Helfer; alle FK-aus-Form-Body-Pfade in 2.x / 3.x sind darauf migriert |

### Code-Hygiene (Epic 3 oder Pre-Prod)

| # | Action | Owner | Kriterium |
|---|---|---|---|
| H1 | **`today_local()`-Helper** in `app/services/_time.py`: liefert `date.today()` in Europe/Berlin. Alle bisherigen `date.today()`-Aufrufe in Aggregations-Services (`due_radar.py`, `registries.py`) migrieren | Charlie vor Story 3.1 | Grep `date.today()` in `app/services/` liefert nur den Helper-Import |
| H2 | **`Severity` als StrEnum** in `app/services/_severity.py`: ersetzt Magic-Strings `"< 30 Tage"`, `"< 90 Tage"`, `"police"`, `"wartung"` | Charlie vor Story 3.1 | Grep auf die String-Literale liefert nur Constants-Datei und Tests |
| H3 | **`deferred-work.md`-Triage**: pro Eintrag `severity` (low / medium / high), `prod_blocker` (yes / no), Owner. Ergebnis-Tabelle als neuer Header-Block | Daniel + Charlie vor Story 3.1 | Datei hat Triage-Tabelle; Pre-Prod-Blocker-Liste ist explizit benannt |

### Architektur-Klärung vor Story 3.6

| # | Action | Owner | Kriterium |
|---|---|---|---|
| A1 | **Approve-Race-Lock-Strategie** für Story 3.6 entscheiden: (a) `db.get(ReviewQueueEntry, id, with_for_update=True)`, (b) Postgres-Advisory-Lock pro `(entity_type, entity_id, field_name)`, (c) Optimistic-Compare-and-Swap auf `version`-Spalte. Entscheidung in `architecture.md` §CD-Review-Queue dokumentieren bevor Story 3.6 erstellt wird | Charlie + Alice vor `bmad-create-story 3.6` | architecture.md hat Sektion "Review-Queue Approve-Race"; Story-3.6-Spec referenziert sie |

### Kritischer Pfad vor Epic-3-Kickoff

| # | Task | Owner | Trigger |
|---|---|---|---|
| C1 | **Live-Verifikation Epic 2** auf https://dashboard.dbshome.de: Police anlegen + Wartungspflicht + Schadensfall + Due-Radar-Klick → Police-Anker scrollt korrekt + Versicherer-Detailseite + Heatmap. Speziell den Story-2.5-Bug-Fix (toter Anchor `#versicherungen` → in 2.6 gefixt) live nachstellen | Daniel | vor Story 3.1 |
| C2 | P3-Helfer `_check_fk_belongs_to_object` | Charlie | vor Story 3.5 |
| C3 | A1-Architektur-Entscheidung Approve-Race | Charlie + Alice | vor `create-story 3.6` |
| C4 | Epic-2-Status auf `done` setzen in `sprint-status.yaml` | Amelia | mit dieser Retro |

### Deferred (vor Produktiv-Rollout, kein Epic-3-Blocker)

- **CSRF-Schutz** projektweit (Starlette-`CSRFMiddleware` oder `hx-headers` mit Token) — in 4 Stories als Defer genannt, betrifft alle POST-Routen
- **DB-CHECK-Constraints** für `praemie >= 0`, `Schadensfall.amount >= 0` — pre-existing Datenmodell-Lücke, betrifft alle Aggregations-Views
- **Optimistic-Lock auf JSONB-Felder** (z.B. `notes_owners`, `state`) für Multi-Admin-Concurrency — `If-Match`-Header oder Versionsfeld
- **`audit_log.ip_address` ohne Length-Cap** — bei malformed `X-Forwarded-For` unbegrenzte Strings in DB
- **Description ohne Length-Cap** auf Schadensfall, Note-Text auf 4000 in Story 2.4 begrenzt aber andere Text-Felder pre-existing unbegrenzt
- **MSAL-Token-Call aus Event-Loop lösen** (Story 1.8 Defer D1, blockiert nur bei SharePoint-Live-Aktivierung)
- **Fernet Key-Rotation-Job** (Story 1.7 Defer)

## Readiness-Einschätzung

| Dimension | Status |
|---|---|
| Story-Completion | ✅ 9 / 9 done |
| Code-Reviews | ✅ alle Patches angewendet, 692 / 692 Tests grün nach Story 2.8 |
| Sicherheits-Findings | ✅ alle CRITICAL / HIGH-Patches angewendet, kein offener Eintrag |
| Live-Deploy | ⚠️ C1 offen — Live-Verifikation auf https://dashboard.dbshome.de steht aus |
| Stakeholder-Acceptance | ✅ Daniel hat alle Stories durchgewunken |
| Technische Gesundheit | ⚠️ ~30 Defer-Einträge, davon ~5 cross-cutting wiederkehrend (H3-Triage offen) |
| Epic-3-Vorbereitung | ⚠️ P3 (FK-Helfer), A1 (Approve-Race-Lock) müssen vor Story 3.5 / 3.6 stehen |

## Nächste Schritte

1. **C1 — Live-Verifikation Epic 2** auf https://dashboard.dbshome.de durchspielen.
2. **H3 — `deferred-work.md` triagen** und Pre-Prod-Blocker-Liste explizit benennen.
3. **C2 + H1 + H2 — Code-Hygiene-Patch-Set** vor Story 3.1: `_check_fk_belongs_to_object` + `today_local()` + `Severity`-StrEnum.
4. **Story 3.1 erstellen** mit `bmad-create-story` — neue Test-Checkliste (P1) + Boundary-Klassifikation (P2) erstmals einsetzen.
5. **A1 — Approve-Race-Lock-Architektur** für Story 3.6 entscheiden bevor `bmad-create-story 3.6` läuft.

## Change Log

- 2026-04-28 — Retrospektive Epic 2 abgeschlossen. 9 Stories done, 692 Tests grün, ~50 Review-Patches angewendet. 3 Prozess-Actions (P1–P3), 3 Hygiene-Actions (H1–H3), 1 Architektur-Klärung (A1), 4 Critical-Path-Items (C1–C4) für Epic 3 dokumentiert.
